# Python dependencies specific to the lingbot_flashvla RoboTwin adapter.
# Install on top of the base RoboTwin conda env (which must already provide
# SAPIEN, mplib, toppra, transforms3d, trimesh, imageio, etc.).
#
# Preferred setup:
#   conda activate <your-flashvla-env>
#   pip install -e /path/to/flashvla
#
# The editable install of flashvla brings in all the runtime deps it declares
# in pyproject.toml (torch, transformers, safetensors, accelerate, lerobot,
# etc.). LingBot's Qwen2.5-VL backbone needs nothing beyond that: the vendored
# modeling code under flashvla/policies/lingbot/ only uses stock transformers.

# Nothing extra at the moment — flashvla pulls in everything we need.
